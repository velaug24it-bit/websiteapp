# Netlify Configuration for Kadalai Mittai Full-Stack Platform
# Root configuration pointing to frontend SPA

[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
